# Rust
/target
**/*.rs.bk
*.pdb
Cargo.lock.orig

# Cross-compilation / build artifacts
/dist
/out
*.deb
*.rpm

# Spec framework (rendered output + tool caches)
/docs/arch/**/_build
/docs/arch/**/*.junit.xml
.spec-cache/
/.spec-reports/
/.work/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*~
*.bak

# Local env
.env
.env.local

# local /metrics capture artifact (verification only)
.metrics_capture.txt
