#!/bin/sh
# prerm — stop and disable the unit before removal.
set -e

case "$1" in
  remove|deconfigure)
    systemctl stop nft_exporter.service >/dev/null 2>&1 || true
    systemctl disable nft_exporter.service >/dev/null 2>&1 || true
    ;;
esac

exit 0
