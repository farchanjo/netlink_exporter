[package]
name        = "nlx-http"
version     = "0.1.0"
edition.workspace    = true
rust-version.workspace = true
authors.workspace    = true
license.workspace    = true
description = "Driving HTTP adapter: Axum server exposing /metrics, /healthz, /ready on port 33400 (ADR-0010)."

# ADR-0023: axum + tokio + tower REMOVED; replaced by monoio hand-rolled HTTP/1.
# monoio-http/service-async deferred (ADR-0023 deviation: hand-rolled approach
# avoids pulling ByteDance-internal crates with limited community docs).
[dependencies]
nlx-ports  = { workspace = true }
monoio     = { workspace = true }
thiserror  = { workspace = true }
tracing    = { workspace = true }

[lints]
workspace = true
