[package]
name        = "nlx-metrics"
version     = "0.1.1"
edition.workspace    = true
rust-version.workspace = true
authors.workspace    = true
license.workspace    = true
description = "Driven metrics adapter: prometheus-client OpenMetrics text exposition implementing MetricRegistryPort (ADR-0006)."

# ADR-0023: prometheus-client retained but not on hot path.
# arc-swap replaces Mutex for lock-free encoded text storage.
[dependencies]
nlx-domain        = { workspace = true }
nlx-ports         = { workspace = true }
arc-swap          = { workspace = true }
thiserror         = { workspace = true }
tracing           = { workspace = true }

[lints]
workspace = true
